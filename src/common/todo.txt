- Add externally configurable file masks for each module
  (ignore masks if called from prefix or plugins menu).

MSI:
- extract progress
- fix problem with real file and non showing the special folders mode

ISO:
- Handle extraction of harddisk emulation boot record (size issues)

PST:
- (?) Handle calendar records more gracefully (not as mail messages)

RELIC:
- Add support for CoH and WH:Dow SGA archives (very similar to H2 BIG-s)
